# Affix Loom — Improvement Notes

Mechanic: weave prefixes/suffixes onto a base word to build the target word.

## Fun
- **Weaving/loom animation**: affixes slide onto the base from left/right and "thread"
  together (expressive easing); the assembled word glows on validity.
- Show morpheme boundaries with subtle dividers that fade as the word becomes whole.
- Win: the completed word does a shimmer pass.

## Addiction
- Morphology is educational → "affixes mastered" collection; streak + build-efficiency.
- Vocabulary-builder framing supports daily habit + EdTech positioning.

## Marketability / share
- Share build steps as a spoiler-safe affix-count pattern; morphology angle → schools/ESL
  (B2B/white-label opportunity noted in the original gap analysis).

## Carbon UI
- Accent → Cyan 50 (`#1192e8`) or Teal.
- Base word `code-02` centered; affix options → `cds-tile` buttons grouped as prefix/suffix
  columns (`$layer-01`, operable `$border-tile-01`).
- Validity → `$support-success` outline; spacing `$spacing-03`.
