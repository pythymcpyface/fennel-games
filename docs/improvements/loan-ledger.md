# Loan Ledger — Improvement Notes

Mechanic: deduce a word's meaning from its morpheme "receipts" (HYDRO=water + PHOBIA=fear).

## Fun
- **Receipt/ledger visual**: present morphemes as line-items on a stylised receipt that
  "totals" into the meaning on a correct match (ledger theme made literal).
- Correct → the receipt stamps "PAID"/✓; the meaning slots in with a satisfying ka-ching
  (optional sound).
- Reduced-motion: instant.

## Addiction
- Vocabulary/etymology learning → "morphemes mastered" collection; strong SAT-prep/EdTech
  daily-habit framing. Streak + attempts.

## Marketability / share
- "Guess the word from its parts" is clever-flex content. Share correct-count row; the
  morpheme receipts screenshot is spoiler-safe and intriguing.
- B2B/education licensing angle.

## Carbon UI
- Accent → Magenta 50 (`#ee5396`) / receipt motif.
- Each item → `cds-tile` with morpheme (`code-01`) + gloss (`body-01`); meaning `cds-dropdown`.
  Locked `$support-success`. Check `$button-primary`. Receipt uses `$layer-01` with
  `$border-subtle-01` dividers.
